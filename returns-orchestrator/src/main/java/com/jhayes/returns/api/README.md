# API Layer
This package contains the **REST Controllers** that serve as the entry points for the Returns Orchestrator.

### Responsibilities:
- Defining HTTP endpoints and request mapping.
- Input validation (using Spring Validation).
- Converting external requests into internal domain signals.
- Handling HTTP response codes and headers.

**Primary Tech:** Spring WebFlux (Netty).