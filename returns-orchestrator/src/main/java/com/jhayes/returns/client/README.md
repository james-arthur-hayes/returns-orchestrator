# External Clients
Contains the WebClient implementations for communicating with external downstream systems.

### Responsibilities:
- Wrapping HTTP calls to Carrier APIs (FedEx, UPS, etc.).
- Handling serialization/deserialization of third-party responses.
- Managing API-specific authentication headers.